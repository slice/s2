[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "s2"
version = "0.0.1"
dependencies = []

[tool.pyright]
include = ["src"]
typeCheckingMode = "strict"
